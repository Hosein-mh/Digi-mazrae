import React from 'react'
import { Root, Cover, Title } from './style';
import walnuts from '../../assets/images/walnuts.jpg';

{/* <Root image={category.picture}>
      <Title>{category.name}</Title>
    </Root> */}
    
export default function CategoryBanner({ category }) {
  return (
    <Root>
      <Title>انواع گردو</Title>
    </Root>
  )
}
